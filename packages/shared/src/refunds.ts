// Upper bound on the freeform "why are you refunding?" answer collected before
// a self-service refund. Shared so the API validation and the dashboard forms
// agree on the limit.
export const REFUND_REASON_MAX_LENGTH = 1000;
