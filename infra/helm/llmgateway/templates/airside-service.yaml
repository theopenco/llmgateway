{{- if .Values.airside.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "llmgateway.fullname" . }}-airside
  labels:
    {{- include "llmgateway.componentLabels" (dict "context" . "component" "airside") | nindent 4 }}
spec:
  type: {{ .Values.airside.service.type | default "ClusterIP" }}
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "llmgateway.componentSelectorLabels" (dict "context" . "component" "airside") | nindent 4 }}
{{- end }}
