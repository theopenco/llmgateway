{{- if .Values.realtime.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "llmgateway.fullname" . }}-realtime
  labels:
    {{- include "llmgateway.componentLabels" (dict "context" . "component" "realtime") | nindent 4 }}
spec:
  type: {{ .Values.realtime.service.type | default "ClusterIP" }}
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "llmgateway.componentSelectorLabels" (dict "context" . "component" "realtime") | nindent 4 }}
{{- end }}
